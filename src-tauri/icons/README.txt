Icons generated with `npx tauri icon`.

The current set (icon.ico + various PNGs) was produced by the official Tauri icon generator from a 32x32 source. This should satisfy both dev and build on Windows.

If you ever see white screen / icon errors in the future, re-run:
  npx tauri icon <path-to-256px-png>
